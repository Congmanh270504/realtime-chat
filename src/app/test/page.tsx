import { CreateServerDialog } from "@/components/server/create-server-dialog";
import React from "react";

const Page = () => {
  return (
    <div>
      <CreateServerDialog />
    </div>
  );
};

export default Page;
