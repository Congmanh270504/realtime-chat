import React from "react";

const Page = async () => {
  //  const [lastMessageRaw] = (await fetchRedis(
  //         "zrange",
  //         `server:${server.id}:messages`,
  //         -1,
  //         -1
  //       )) as string[];
  return <div>Page</div>;
};

export default Page;
